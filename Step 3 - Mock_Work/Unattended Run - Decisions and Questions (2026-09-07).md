# Unattended Run - Decisions and Questions (2026-09-07)

Run: widget-final-check-audit, unattended, owner-authorised run-to-completion. Ground truth: the Final Check tab slices (fc-widget-13, purF code, .purf-root CSS), the Step 4 doc, and the Step 6 dossier (Confluence pull 2026-07-27; no reconciliation file exists).

## W13 - Purchasing Management

### Applied automatically — doc caught up to build (reversible, review if you like)
- [ ] H1 Glance headline vocabulary · Step 4 doc, Headline math bullet · was: `The Glance card title reads the live "N awaiting my approval next" figure. [BUILD, 2026-08-19]` · now: `The Glance card title reads the live "N pending approval" figure ('FC_KPI_HEADLINE[13]' renders "Purchasing: N pending approval", N = count of Pending-state requests; the pre-v2.2 "N awaiting my approval next" wording is superseded). [BUILD, checked in code 2026-09-07]` · Reason: the code returns 'Purchasing: '+n+' pending approval' with n = Pending count; the doc's own Size table already said "N pending approval", only this bullet was stale.
- [ ] H2 Status line build version · Step 4 doc, Status line · was: `...v2.2 same day: approval-process Kanban with drag-to-action` · now: `...build rounds v2.1 through v2.6 the same day, ending at the approval-process Kanban with drag-to-action, colour-state cards, record-screen-parity modal and split Finish column; 'FC_VERSION[13]' = 2.6, checked 2026-09-07` · Reason: FC_VERSION[13] is '2.6', not 2.0/2.2; all six rounds are dated 2026-08-19 in the build changelog and the doc body already describes them, only the header understated.
- [ ] H3 Size table, Explore row · was: `Kanban: all 4 columns, top 2 POs each` · now: `Kanban: the 3 state columns plus the split Finish column (Finish shows only when the status filter is "All statuses"), top 2 POs per state column` · Reason: "all 4 columns" was four-stage-era wording; the code renders repeat(3,1fr) state columns plus a 0.5fr Finish column gated on status === All statuses, cap 2 at Explore.
- [ ] H4 Size table, Detail row · was: `Kanban: all 4 columns, full cards, column scroll` · now: `Kanban: the 3 state columns plus the split Finish column (same "All statuses" condition), full cards, column scroll` · Same reason as H3.
- [ ] Stamp updated to `2026-09-07 via widget-final-check-audit (unattended)`, previous 2026-08-19 build-final-widget stamp preserved as Previous.

### Blocked on a fact — could not be defaulted
- [ ] DISPUTED (recorded both-sides in the doc already, untouched): payment approval in the widget. Feargal (2026-08-25 call, item 3): preserve existing payment functionality, redirect to the established payment screen; supersedes v2.4/v2.5. Owner-directed build (2026-08-19, v2.4/v2.5): payment entry reproduced in the record modal (invoice grid, Submit for Approval, card turns green). Held pending confirmation (action list item C3). Who settles: Feargal Phelan / owner, on C3. No winner picked; the build was not changed.
- [ ] UNRESOLVED (both positions stand): voided requests. Feargal item 5 says the design must not assume the underlying financial reversal process; the v2.6 build already implements Void with assumptions (unpaid only, confirm + reason, terminal, "check reversal is an AP flow" toast). Doc records "needs review before more is built on top". Who settles: Feargal Phelan.
- [ ] UNREVIEWED dossier flags with no status row in the doc's Sign-off Input section: 11.6 (accessibility parity: chart values as text, named action icons) and the "stable columns" half of 11.5 (aging half is Accepted). The build largely satisfies both, but no Accepted/Rejected/Disputed status is recorded. Owner: Oisin, assign a status or add the rows.
- [ ] Dossier open question still open: "re-confirm live whether chart values are text or hover-only" (edit-icon navigation half was settled 2026-08-19). Owner: Oisin/Jo.
- [ ] `[TO CONFIRM]` rows unchanged, still open with named owners: Department / Year / Overdue as real backend fields (rows 1-3, owner TBD); payment-side hold (row 12); approve/reject write API (row 10); rows 7-9, 11, 13 backend prerequisites, all owner-accepted forward design.
- [ ] No Step 5 API spec exists for Purchasing Management (no `Step 5 - API documents/Purchasing Management/` folder). All backend needs are scoped only in the Step 4 Sign-off Readiness table (rows 7-13). Question: write the V2 API spec next, as done for W01-W07, W09-W11?
- [ ] Build-side nits (not doc problems, not edited; fix in the mockups file when next touched): the code comment above `purFGlance` still says "four compact stage-count cards" (renders three); the comment above `FC_KPI_HEADLINE[13]` still quotes "N awaiting my approval next" (string is "N pending approval"); the fc-widget-13 `w-desc` paragraph stops at "v2.2 same day" while the Logic section carries v2.3-v2.6.
- [ ] `.btn` family check (W03 bug class), verified clean: purF markup uses no `.btn` classes at all; every button is purf-scoped (`.purf-m-close`, `.purf-m-primary`, `.purf-ovbtn`, `.purf-vtog-btn`, `.purf-more`, `.purf-addlink`) and all are declared under `.purf-root`. No unstyled-export risk here. Side note from the W15 changelog: `.purf-sel` carries hardcoded hexes (not tokenised); cosmetic, owner's call whether to tokenise.

## W16 - Accounts Payable By Due Date

Ground truth: fc-widget-16 slice, apF/APF_ code slice, .apf-root CSS slice, FC_VERSION[16] changelog ('2.0', single round dated 2026-08-19 - no rounds after the doc's 2026-08-19 stamp), Step 4 doc, Step 6 dossier 7371554882 (Confluence pull 2026-07-27; no reconciliation file, and the doc has no Sign-off Input (Jo) section, so every dossier flag is Unreviewed).

### Applied automatically — doc caught up to build (reversible, review if you like)
- [ ] H1 Dossier pointer · header line 7 · was: `**Confluence dossier:** none yet` · now: link to the Step 6 pull, page 7371554882, with a "no reconciliation file yet / no statuses yet" note. Reason: the dossier exists and the doc's own Fine-Tuning entry already cites it.
- [ ] H2 Filters section · was: Due Date = All / Overdue Now / Due This Week / Due This Month + Vendor chip; "filtering narrows the table only; the donut always shows all due dates" · now: the built due-date popover (All due dates / By aging incl. the fourth "Due later" band / By specific date with search past 8 dates) plus the horizon chip (All outstanding / Next 7-30-60-90, overdue always included, snap rule); no Vendor chip (vendor search past 6 rows + Top vendors rollup); filtering is widget-wide; due/horizon change is the only fetch; Glance has no filters.
- [ ] H3 Data Table Sort · was: "Fixed - Due Date ascending, then Vendor alphabetical... Not user-changeable" + Small/Medium trimmed-view rule · now: every column sortable, default due-asc with DATE TIES BROKEN BY LARGER AMOUNT FIRST (code: `dir*(dd||(b.amount-a.amount))`), band-grouped at All due dates; no trimmed-view rule (no Small tier in the Final).
- [ ] H4 Views section · was: View 1 Due Date Cards (default) / View 2 Aging Donut / View 3 AP Table · now: no Switch View in the Final; hero aging-band grouped table + Detail cash-requirements/top-vendors panel; old views survive only under A/B/C.
- [ ] H5 Size behaviour table · was: Small/Medium/Large/KPI/Expanded, 3/5 cards, "Total AP Outstanding across all due dates" · now: Glance/Explore/Detail per Jo's three-tier model; Glance = scope chip + total + overdue/nothing-overdue pill; KPI figure computes from the HORIZON-SCOPED set, not unconditionally all due dates.
- [ ] H6 Widget States, Empty row · was: "Not yet specified" · now: built "Nothing outstanding" positive state, short variant at Glance.
- [ ] H7 Widget States, Loading row · was: "Not yet specified" · now: 800ms header skeleton + 5 skeleton rows, "Updating outstanding invoices...", fetch on due/horizon change only; initial-load still unspecified.
- [ ] H8 Interaction Spec donut bullets · was: donut hover/click, adoption unspecified · now: no chart in the Final; bucket click adopted as selector rows (cash-panel band rows + popover entries); sortable headers with aria-labels.
- [ ] H9 Accessibility bullets · was: three "Not yet reviewed against the build" claims · now: reviewed 2026-09-07 - colour-never-alone MET, values-as-text MET, table semantics NOT MET (div-based .wt-row, no table/columnheader roles or th/scope; popovers do use listbox/option). Decision on requiring role=table parity left [TO CONFIRM - Oisin].
- [ ] H10 Fine-Tuning bullet "Due Date filter filters all views independently" marked as A/B/C-era.
- [ ] Stamp updated to `2026-09-07 via widget-final-check-audit (unattended)`, previous 2026-08-19 build-final-widget stamp preserved as Previous.
- [ ] `.btn` family check (the W03 bug class named in W03's audit), verified clean: the apF markup uses zero `.btn` classes (buttons are filter-chip / wt-sort / mi / ap-sum-row, all declared under `.apf-root`, popovers carry .apf-root too). No unstyled-export risk for W16; no CSS edit needed.

### Blocked on a fact — could not be defaulted
- [ ] DISPUTED-SHAPED, recorded both sides, no winner picked: actionability. Jo's dossier 11.2 + gap table row 1 [SME - Marvin 13 Jul 2026]: "make it payable-actionable (pay/schedule/one-step check, Payroll STO inline-action pattern) - the single biggest reason to use it", effort L, marked "Phase". Step 4 doc [Feargal call 2026-08-25]: this widget stays a focused due-date/aging overview, separate action widgets assigned to Aditya. Build: view-only, no actions, no drill-through. Who settles: Oisin with Feargal Phelan and Jo (and Aditya's queue).
- [ ] UNREVIEWED: dossier 11.3/11.4 call for a DATE-ORDERED CHART synced to the filter; the Final (copied 1-to-1 from Jo's own later v2 build) has NO chart at all. Sync is satisfied widget-wide, the chart itself is not. Accept "no chart" as superseding 11.3, or record a deviation? Owner: Oisin/Jo.
- [ ] UNREVIEWED: dossier 11.6 "pair with available cash (Bank Balances) for a can-we-cover-it read" - absent from doc and build. Accept/reject/defer? Owner: Oisin.
- [ ] UNREVIEWED: dossier 11.7 + live audit: the GBP hard-coded currency defect [LIVE 23 Jul 2026]; the doc's currency/locale row says "Not yet specified" and the mock formats en-US "$". Record the defect in Data Contract / Sign-off Readiness? Owner: Oisin (fix itself is backend/dev).
- [ ] UNREVIEWED: dossier 9 asks for Refresh with a "data as of" timestamp; doc says freshness not specified; build's refresh is shared fc chrome (toast only). Owner: Oisin.
- [ ] Dossier open question still open: confirm the aging basis is DUE DATE, not invoice date. The build's band math uses due date only (`apFDiffDays(x.due)`), consistent, but never confirmed against the real API. [TO CONFIRM - dev, with Jo's 13. Open questions]
- [ ] No Sign-off Input (Jo) section exists in the Step 4 doc and no `Reconciliation - Accounts Payable By Due Date.md` exists in Step 6. Every dossier flag above is therefore statusless. Question: add the section / write the reconciliation file next? Owner: Oisin.
- [ ] Still-open items carried forward unchanged: drill-through (Sign-off row 1, experts/dev); W10 "Over 60 = 90+" mislabel carryover check (row 2, TBD); Modern API entitlement not enforced (row 3, dev, must be fixed server-side before release); error state, initial-load, rounding/locale rules, "data as of" freshness all still unspecified.
- [ ] No Step 5 API spec folder exists for Accounts Payable By Due Date; backend needs (horizon parameter, band math, entitlement fix, currency fix) are written down only in Step 4. Question: write the V2 API spec next, as done for W01-W07, W09-W11? Owner: Oisin.
