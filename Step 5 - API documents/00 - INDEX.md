# Step 5 — API Documents — Index

> **Read this file first.** This folder is where a widget's backend needs get properly specified, once its design is finished. Renamed from `API documents` on 2026-07-20 to bring it in line with the project's Step-N naming (`Step 2 - Feedback`, `Step 3 - Mock_Work`, `Step 4 - Widget Final Design`, now `Step 5 - API documents`). All project references to the old name were updated at the same time.

## Where this fits in the pipeline

**A widget only starts here once it's marked ✅ Done in `Step 4 - Widget Final Design/00 - INDEX.md`.** Being "in progress" or "close to done" in Step 4 is not enough — an API spec is the next step after a widget's design is finished and confirmed against the real build, not a prerequisite for reaching that point. So the widgets eligible for a spec in this folder grow as Step 4's Done list grows.

**Sign-off findings check (added 2026-07-27):** before writing or updating any spec here, check the widget's `Step 6 - Sign off document/` subfolder — as of 2026-07-27 every widget has a pulled Confluence dossier there, and some have reconciliation files. Findings carry a status: **Accepted** ones must be honoured in the spec (don't spec fields for a feature that was accepted as rejected); **Rejected** ones are noted and ignored; **Disputed** ones (both sides hold evidence, e.g. W03's pay-type list) go into the spec's "Still needs sign-off" section with both claims cited — the spec never picks a side; **Unreviewed** ones need the project owner's status call before speccing the part they touch. The `widget-api-spec-writer` skill encodes this. W03's reopening (see bottom of this file) is the precedent: a sign-off finding invalidating a spec assumption is exactly what this check exists to catch early.

## Reference docs (read-only — do not edit)

- **`Widget_Comparison_Classic.html`** and **`Widget_Comparison_New_Widgets.html`** — these exist purely as a historical comparison of what used to be there (the legacy ASP.NET codebase and tables) against the modern API already built to replace it, per widget. They are not living documents and are not meant to be updated as new specs get written — they're the fixed baseline every new spec should check against, not a target to keep in sync.
- **`How to Write a Widget API Spec.md`** — the process to follow for every new spec: find the real existing API first (in the two comparison docs above), list tables, show old vs. new, define request params and a full real JSON response example, edge cases, open items, and scope boundaries. API only — no frontend concerns (sizes, colors, chart types) belong here.
- **`Pending Questions - Codebase Findings (2026-07-30).md`** — findings from a read of the MBAccounting codebase against the specs' open questions, dated 2026-07-30. A working reference feeding the per-widget open items, not a spec itself. *(Entry added 2026-08-19 by the tracker-keeper audit; the file existed but was never indexed.)*

## Widget specs

| Widget | Step 4 status | Folder | Spec status |
|---|---|---|---|
| Budget Compared to Actual (W01) | ✅ Done | [Budget Compared to Actual](Budget%20Compared%20to%20Actual/) | ✅ Complete (spec file still carries its internal DRAFT header pending sign-off) — rewritten 2026-07-27 as a clean dev handoff (Time Window Module contract: window/grain/asOf, bucket response, validation matrix); Confluence HTML regenerated |
| Pension Plans (W02) | ✅ Done | [Pension Plans](Pension%20Plans/) | ✅ Complete (spec file still carries its internal DRAFT header pending sign-off) — written 2026-07-27, revised same day to a strict 1-to-1 contract with the existing pension-plans endpoints; only new field: AppointeeCount on grid rows (plus the pre-existing Charge bug fix); grouped-bar matrix and all-districts scope assembled client-side |
| Payroll Distributions (W03) | ✅ Done | [Payroll Distributions](Payroll%20Distributions/) | ✅ Complete (spec file still carries its internal DRAFT header pending sign-off) — updated 2026-07-27 for the built Final (window/custom-range params; pay-type breakdown feasibility RESOLVED with evidence, see the Pay Type Breakdown Analysis proof file in the folder; no comparison fields per sign-off F3; **zero personal data by owner decision**: amounts only, no hours/rates/names/check numbers, breakdown stops at distribution × pay type) |
| Remittance Pledges (W04) | ✅ Done | [Remittance Pledges](Remittance%20Pledges/) | ✅ Complete (spec file still carries its internal DRAFT header pending sign-off) — **rewritten 2026-08-08 as one clean doc with TWO APIs**: (1) Dashboard summary (per-activity pledged/expected/paid/outstanding/%paid, per-pledge-term pacing) and (2) Activity Payment History popup (the "more information" drill-down: summary tiles + the receipts list Date/Reference/Amount/Status from `RM_HistoryDetail`+`RM_History`+`RM_HistoryBatch`, with reference SQL). Every field classified stored vs derived, verified against the code (two-agent build+verify pass); receipt Amount is `RM_HistoryDetail.Amount` not the whole-check `RM_History.Amount`; status pill/narrative/expected are derived, not stored. Companion provenance docs sit in the same folder (Legacy Behaviour + Payment History Popup Field Source Map). Open items: pacing definition (linear-by-days as built vs stepped `Frequency`/`Duration` vs the stored `RM_PledgePercent` schedule, unused today); ended-but-unpaid pledge inclusion |
| Receivable Invoices Outstanding (W05) | ✅ Done | [Receivable Invoices Outstanding](Receivable%20Invoices%20Outstanding/) | ✅ Complete (spec file still carries its internal DRAFT header pending sign-off) — written 2026-07-28 for the built Final, structured as three APIs (widget aging summary + invoice count; pop-up bucket list + the open move-to-unposted Confirm action; per-invoice drill-in detail); move-to-unposted transaction type is the open SME/API item, see the logic-notes file |
| Insurance Billing Plans (W06) | ✅ Done | [Insurance Billing Plans](Insurance%20Billing%20Plans/) | ✅ Complete (spec file still carries its internal DRAFT header pending sign-off) — revised 2026-08-04 to ONE nested call (owner-directed): a single `GET .../insurance-billing/plans` returns `total` plus `types[]`, each type carrying its name, its subtotals, and its `plans[]` inside it (type→plans in one response); the separate `.../types` endpoint is removed and the type filter is now a client-side view over the response, not a fetch. `enrolled` is the existing read; Cost per plan is the one new field = SUM of IBEmployeePlan.Rate; only Share stays client-side. Open: total-vs-employer-vs-employee cost definition, and dependents-in-count-not-in-cost |
| Deposit Accounts (W07) | ✅ Done | [Deposit Accounts](Deposit%20Accounts/) | ✅ Complete (spec file still carries its internal DRAFT header pending sign-off) — updated 2026-08-04 for the built "Deposits on Hand" Final (folder/file keep the legacy name pending the project-wide rename). **Pagination made an accepted requirement** (owner-directed): server-side, 50 accounts/page, `page`/`pageSize`, response carries `pagination.totalAccounts`, and `total`/type subtotals always compute over the full filtered set (KPI, donut, trend read those, never the page) — the built Final demonstrates it with 125 accounts / $106,726,837. `compareTo` aligned to the built six-option scale (Previous Week / Month / Period / Quarter / Fiscal Year / Calendar Year; fiscal **Period** sits between month and quarter); `range` follows the Time Window Module grains (D W M P Q Y). Scope-dependent breakdown + click-to-drill documented as client-side re-scoping over the same response (no new endpoints). Open items reconciled to Step 4: historical period-end balances gate Trend/Compare To, out-of-widget drill-through target, and the balance tie-out. One-vs-split-endpoint (Open Q1) still open. Confluence HTML regenerated |

All other widgets (W09–W17) aren't eligible yet — none are marked Done in Step 4. **W06 became eligible when its Final was built (Step 4 Done as of the built Final)** and now has a draft spec here — it is the **7th widget** with a draft spec (W01, W02, W03, W04, W05, W06, W07). (W05 and W06 each became eligible when their Finals were built; W04 became eligible on 2026-07-28 when its Final was built and its Step 4 doc was tagged v2.0.)

## V2 specs

The widgets are being redesigned, so specs are being rewritten rather than patched. A V2 spec lives in the widget's own `v2/` subfolder; the V1 spec stays at the folder root, frozen and read-only. V2 specs are **design-only** — no dates, no change narration, no version numbers in the body. Everything historical for a widget lives in that widget's `DECISIONS.md`, outside the spec.

Toolkit in `v2-toolkit/`: `TECHNICAL-FRAMEWORKS.md` (the four decision frameworks — filter execution, volume, API decomposition, computation location), `SPEC-TEMPLATE-V2.md` (section order and per-section requirements), `spec_lint.py` (the static gate; run it before any handover), `SPEC-QUALITY-RUBRIC.md` (the 30-point scoring rubric).

Rows state current state only.

| Widget | Build version | V2 spec | Lint | Notes |
|---|---|---|---|---|
| Budget Compared to Actual (W01) | v2.0 | ✅ Done - awaiting sign-off · [v2/](Budget%20Compared%20to%20Actual/v2/) | HIGH 0, MED 0 | Three APIs: one bounded data read (Time Window Module contract: window/grain/asOf, buckets plus a posted-only `total` with pre-signed variance) and the two existing special-report lookups extended by `lineCount` (reports) and `type` (lines). Nothing paginates. `total` basis is posted-buckets-only per the built Final; `ActualYTD`/`BudgetYTD` and export dropped. Open facts: day/week actuals posture, sub-period budget derivation, master-company aggregation detail, line-kind filter, lookup volume ceilings [TO CONFIRM] (Feargal); dossier flags F1-F9 all Unreviewed, listed in Still needs sign-off. History and rulings in [Budget Compared to Actual/DECISIONS.md](Budget%20Compared%20to%20Actual/DECISIONS.md) |
| Pension Plans (W02) | v2.2 | ✅ Done - awaiting sign-off · [v2/](Pension%20Plans/v2/) | HIGH 0, MED 0 | Three APIs: district lookup, one reshaped summary read (plan rollup + appointeeCount + per-district cells in a single response, replacing the V1 fan-out posture), and the existing appointee-detail drill. Chart endpoint out of contract. Volume ceilings [TO CONFIRM] (Marvin) condition the BOUNDED verdicts; dossier findings all Unreviewed, listed in Still needs sign-off. History and rulings in [Pension Plans/DECISIONS.md](Pension%20Plans/DECISIONS.md) |
| Payroll Distributions (W03) | v2.6 | ✅ Done - awaiting sign-off · [v2/](Payroll%20Distributions/v2/) | HIGH 0, MED 0 | Four APIs: distribution filter lookup, bounded distribution-by-pay-type summary, paginated permission-gated employee drill, server-side export. No comparison fields (sign-off F3). The employee level supersedes the V1 spec's zero-personal-data boundary, per the Feargal call and the owner's nested-drill build. Open facts: `this_period` payroll semantics, the exact payroll permission right, PR_Employee columns, volume ceilings. History and rulings in [Payroll Distributions/DECISIONS.md](Payroll%20Distributions/DECISIONS.md) |
| Remittance Pledges (W04) | v3.5a | ✅ Done - awaiting sign-off · [v2/](Remittance%20Pledges/v2/) | HIGH 0, MED 0 | Four APIs: bounded per-activity pacing summary (range pair, `windowPaid`, per-row `shortfall`), paginated per-activity pledge list serving both the drill screen and the top-5-behind popup (`behindOnly`, full-set summary block, sort fixed at shortfall desc + pledgeId tiebreak), per-pledge payment schedule (oldest-first allocation, first skipped date), and a view-scoped server-side export. Receipts-only mode carried as design intent, gated on Step 4 Sign-off Readiness row 7 (open, blocking that path only). Open facts: Frequency/Duration semantics, per-pledge receipt attribution, linear-vs-stepped expected (RM_PledgePercent), pledge volume, permission right. History and rulings in [Remittance Pledges/DECISIONS.md](Remittance%20Pledges/DECISIONS.md) |
| Receivable Invoices Outstanding (W05) | v2.3 | ✅ Done - awaiting sign-off · [v2/](Receivable%20Invoices%20Outstanding/v2/) | HIGH 0, MED 0 | Six APIs. Two owner rulings needed to write it: six aging bands (build over doc), and the drill modal specced as built. Scope risk on the record: if the navigate-out question resolves toward the Step 4 doc, API 4 and API 5 are both dropped. Step 4 doc is stale on band count |
| Insurance Billing Plans (W06) | v2.4 | ✅ Done - awaiting sign-off · [v2/](Insurance%20Billing%20Plans/v2/) | HIGH 0, MED 0 | Two APIs: the existing insurance-type lookup reused unchanged to feed the filter chip, and one bounded nested data read (types, their plans, subtotals and a grand total) with `typeId` as a server param. Read-only; no pagination, no time axis, no write. Chart renders client-side off the same rows as the table. `cost` is the one NEW field; the existing grid and chart endpoints are out of contract. Fetch posture reverses V1's owner-instructed fetch-once shape and is the likeliest disagreement here (sign-off item 4; response shape is identical either way). Explore default view unconfirmed, Table stays default. Volume ceilings [TO CONFIRM] (Marvin); dossier findings all unstatused. History and rulings in [Insurance Billing Plans/DECISIONS.md](Insurance%20Billing%20Plans/DECISIONS.md) |
| Deposit Accounts (W07) | v2.0 | ✅ Done - awaiting sign-off · [v2/](Deposit%20Accounts/v2/) | HIGH 0, MED 0 | Four APIs: scope lookup (server-searched account typeahead), bounded balances summary, paginated/server-sorted/server-searched account rows, and the balance series (grain gap; computed on demand per the standing ruling). Closes V1's open one-vs-split question by framework. Build-vs-doc discrepancy on a Trend comparison overlay carried unresolved in Still needs sign-off, alongside the Disputed dossier grain finding (both sides cited, no winner). Volume ceilings and balance tie-out [TO CONFIRM] (Marvin / backend). History and rulings in [Deposit Accounts/DECISIONS.md](Deposit%20Accounts/DECISIONS.md) |

| Loans With Balance Due (W10) | v3.1 | ✅ Done - awaiting sign-off · [v2/](Loans%20With%20Balance%20Due/v2/) | HIGH 0, MED 0 | First ever spec for this widget; no V1 exists. Four APIs: portfolio summary (Glance fires this alone), paginated loan list (`MUST PAGINATE`, no citable ceiling, so sort/filter/totals are server-side), loan-type lookup (lifetime gap, TTL 15 min), and loan detail carrying its own payment history. Server owns the five range ladder and always returns all five including zeros, which funds the muted inert $0 row. Two fact gates open and BLOCKING, both resurfaced after their v3.0-only waiver: no backing field for Status (not built, not specced), and the Modern API not replicating the legacy oldest-first payment allocation, which every range figure depends on. A third divergence found while writing: one range per loan versus legacy's per-invoice split, so the donut will not equal the legacy pie even once LIFO lands. Owner has ruled Feargal item C2 non-blocking for this build. Likeliest disagreement is the pagination verdict. History and rulings in [Loans With Balance Due/DECISIONS.md](Loans%20With%20Balance%20Due/DECISIONS.md) |

| Payroll Scheduled Time Off (W09) | v3.3 | ✅ Done - awaiting sign-off · [v2/](Payroll%20Scheduled%20Time%20Off/v2/) | HIGH 0, MED 0 | First ever spec; no V1 exists. **The only spec in the batch with a real write**, and its endpoint does not exist: `PUT /entries/{id}/approval` is NEW, specced as a desired-state write so idempotency comes by construction. Seven APIs: labels, summary, paginated queue, calendar month map, one date's people, per-employee context, and the write. Three Modern API gaps all NEW: the approval-authority filter is not implemented (so every supervisor would currently see every employee, a confidentiality gap, and the contract applies authority to reads as well as the write), no inline approval endpoint, and no `PRCompany` custom leave-type labels. Queue paginates at PERSON grain, following v3.0's deliberate cap unit. Outstanding is a DERIVED state, not stored. Two undocumented build behaviours found in code and specced: the queue's pending counts ignore the Status filter, and the calendar's department option list spans the dataset while its counts are month-scoped. Likeliest disagreements: seven APIs may read as over-decomposition, and the queue paginating is the largest gap between the mock's feel and the contract. History and rulings in [Payroll Scheduled Time Off/DECISIONS.md](Payroll%20Scheduled%20Time%20Off/DECISIONS.md) |

| Fixed Asset Values (W11) | v3.6 | ✅ Done - awaiting sign-off · [v2/](Fixed%20Asset%20Values/v2/) | HIGH 0, MED 0 | First ever spec; no V1 exists. Confluence HTML deliberately deferred pending owner review of the draft .md. Five APIs: org summary (Glance fires this alone; the org-wide Net Value summation is stated in no source), group totals (merges the existing values + chart reads; zero-total groups included so the client can name them, per-group `assetCount` NEW, `dollarType` dropped), paginated server-sorted asset grid (`MUST PAGINATE`; the sort/page/totals contract exists nowhere today and is the widget's central NEW backend work), and a preferences read/write pair (Modern API persists nothing today). Financial Measure is the one CLIENT filter, legitimate because all five measure totals are served per group. Fact gates carried, not resolved: the register is illustrative mock data never verified against real fixed-asset tables; six-dimension/dependent-group/five-measure feasibility unconfirmed; the three account dimensions return an empty list today; default view contested (handoff says Donut, build opens Asset Detail; spec follows the build); dossier flags J1-J7 all Unreviewed; volume ceilings [TO CONFIRM] (backend team). Download is an unfunded gap, possible sixth API. History and rulings in [Fixed Asset Values/DECISIONS.md](Fixed%20Asset%20Values/DECISIONS.md) |

| Bank Balances (W15) | none recorded (no `FC_VERSION[15]`; the bkF block's dated comments, rebuilt 2026-09-04, are the build's only version record) | ✅ Done - awaiting sign-off · [v2/](Bank%20Balances/v2/) | HIGH 0, MED 0 | First ever spec for this widget; no V1 exists. Confluence HTML deliberately deferred pending owner review of the draft .md. Three APIs: paginated/server-sorted/server-aggregated accounts page (MUST PAGINATE per Ben Lane's 50+ accounts; page size 12, sort whitelist has the single member `name` with id tiebreaker, `overdrawnOnly` param, full-set totals + `maxEnding`/`minEnding` + whole-active-set `overdrawnCount`; Glance reads its aggregates alone), single-account seven-line activity breakdown (entirely NEW - the Modern API returns a summary balance only, the waived Rule 11 backend ask, blocks Single Account mode), and a full-set picker lookup with balances (BOUNDED conditional on the unconfirmed ceiling; typeahead fallback recorded). API 1 exists in NO Modern API doc and is central NEW work. Read-only, no writes, no time filter, no download. Fact gates carried: dossier findings ALL Unreviewed (no reconciliation file), incl. the live pound-sign localisation defect and the available-vs-unrestricted-cash "Do now"; SME attribution conflict (dossier "Marvin" vs Step 2 Ben Lane) both-sides; top-3-5-plus-view-all vs paged full set open; `pageSize` max 100 defaulted for owner confirmation; account ceiling and permission right [TO CONFIRM] (backend team). History, defaults and rulings in [Bank Balances/DECISIONS.md](Bank%20Balances/DECISIONS.md) |

| Accounts Payable By Due Date (W16) | v2.0 | ✅ Done - awaiting sign-off · [v2/](Accounts%20Payable%20By%20Due%20Date/v2/) | HIGH 0, MED 0 | First ever spec for this widget; no V1 exists. Confluence HTML deliberately deferred pending owner review of the draft .md. Four APIs: bounded aging summary (Glance fires this alone; horizon-scoped, never due-selection-scoped), paginated invoice list (MUST PAGINATE, no citable ceiling, so sort/vendor-search/band-subtotals/totals are all server-side; due-asc default, amount-desc tie, invoiceId tiebreaker), due-date facet feeding the popover (replaces the modern /filters read, adds totals/counts/day offsets; BOUNDED conditional on an unconfirmed distinct-date ceiling), and a top-vendors rollup (Detail tier only). The existing /chart endpoint is dropped (no chart in the Final); read-only, no writes. TOP open item: the aging basis (due date vs invoice date) was never confirmed against the real API and is load-bearing for every band. Actionability (dossier 11.2 SME pay/schedule ask vs Feargal focused-overview ruling) carried as a recorded dispute, read-only specced; dossier 11.3/11.4/11.6 Unreviewed; entitlement enforcement, GBP localisation and asOf freshness are NEW/UNVERIFIED backend work; X-BankAccountID-vs-X-Company-ID header oddity flagged; div-table accessibility parity [TO CONFIRM - Oisin]. History, defaults and rulings in [Accounts Payable By Due Date/DECISIONS.md](Accounts%20Payable%20By%20Due%20Date/DECISIONS.md) |

| Purchasing Management (W13) | v2.6 | ✅ Done - awaiting sign-off · [v2/](Purchasing%20Management/v2/) | HIGH 0, MED 0 | First ever spec for this widget; no V1 exists. Confluence HTML deliberately deferred pending owner review of the draft .md. Seven APIs: state-count summary (Glance fires this alone, path-scoped only), capped per-state approval board (BOUNDED by construction, cardsPerState max 100), paginated request table carrying the Closed/Voided archive (MUST PAGINATE, no citable ceiling, sort/filter/totals server-side, oldest-first default with orderId tiebreaker), the existing approval-paths lookup reused with a `state` param, and three NEW writes: state transition (approve/reject/return/close/void with reason and server-enforced guards), hold toggle, and the payment-approval entry, which is specced from the built behaviour but gated DISPUTED (Feargal redirect-vs-rebuild, held on C3; if redirect wins, API 7 drops). Voided-request financial treatment carried both-sides, no reversal behaviour asserted. Rejected orders, paymentStatus, hold read/write and archive serving are all NEW backend asks (Step 4 rows 9-13); Department/Year/Overdue are UNVERIFIED Rule 11 fields that drop cleanly if not real (rows 1-3); write-authority rule and volume ceilings [TO CONFIRM] (Feargal Phelan); dossier 11.6 and the stable-columns half of 11.5 are Unreviewed, listed in Still needs sign-off. History, defaults and rulings in [Purchasing Management/DECISIONS.md](Purchasing%20Management/DECISIONS.md) |

| Gifts Pledges (W17) | v1.3 | ✅ Done - awaiting sign-off · [v2/](Gifts%20Pledges/v2/) | HIGH 0, MED 0 | First ever spec for this widget; no V1 exists. Confluence HTML deliberately deferred pending owner review of the draft .md. Five APIs: bounded campaign summary (Glance and both views read it; adds pledgeDue/dueRemaining/percentDue/goal/progressPercent/isClosed + totals to the modern 4-field DTO, nearly all NEW server work), campaign lookup (TTL 15 min), paginated donor pledge list (MUST PAGINATE, fixed most-behind-first sort + pledgeId tiebreaker, `behindOnly` serves the top-5 modal off the same endpoint with a full-set summary block), per-pledge gift transactions, and a view-scoped server-side export (the widget's ONLY action, per the standing export-only constraint). rangeEnd is the sole date param (range start is chip display only, no figure windowed by it). TOP open item: Received basis conflict, build's pledge-linked-gifts-only vs the Modern API's all-posted-gifts (gifts-pledges DTO and campaign-giving-tracker TotalRaised both), blocking every summed figure. Also carried: Goal keyed on GF_Campaign vs GF_Purpose mapping [TO CONFIRM]; modern ProgressStatus "red (goal met)" vs the design's positive goal-met band (spec serves raw numbers, backend to confirm the enum is ignorable); Step 4's internal % Due inconsistency (CONFLICT row vs RESOLVED readiness note) recorded not collapsed; dossier flags ALL Unreviewed (no reconciliation file), incl. the pound-sign localisation defect and entitlement behaviour; "Open in Gifts and Pledges" stub vs export-only constraint [TO CONFIRM - Oisin]; volume ceilings [TO CONFIRM] (backend team). History, defaults and rulings in [Gifts Pledges/DECISIONS.md](Gifts%20Pledges/DECISIONS.md) |

All 14 in-scope widgets now have v2/ specs (status: Done - awaiting sign-off, per direct instruction 2026-09-07); V1 files where present are frozen history. W01's V1 spec at the folder root is frozen; its `v2/` spec is the working contract.

## Step 5 tracker status — all 17 widgets

`Dashboard Tracker.xlsx`'s "Step 5 - API documents" column — **re-synced 2026-08-04: W01-W07 are all ✅ Complete** (per direct instruction, all seven having built Finals and completed the pipeline through Step 5). Earlier syncs (2026-07-20/21) had W01/W07 "In progress" and the rest "Not started"; that is superseded. Remaining 10 "not eligible" widgets (W08-W17) are listed too so the full 17 are covered in one place:

| # | Widget | Step 5 (tracker) |
|---|---|---|
| 01 | Budget Compared to Actual | ✅ Complete |
| 02 | Pension Plans | ✅ Complete |
| 03 | Payroll Distributions | ✅ Complete |
| 04 | Remittance Pledges | ✅ Complete |
| 05 | Receivable Invoices Outstanding | ✅ Complete |
| 06 | Insurance Billing Plans | ✅ Complete |
| 07 | Deposit Accounts | ✅ Complete |
| 08 | My Status | ✅ Complete (by Jo, out of scope) |
| 09 | Payroll Scheduled Time Off | ✅ Complete |
| 10 | Loans With Balance Due | ✅ Complete |
| 11 | Fixed Asset Values | ✅ Complete |
| 12 | *(Empty Slot)* | ➖ N/A |
| 13 | Purchasing Management | ✅ Complete |
| 14 | Main Content Tasks | ✅ Complete (by Jo, out of scope) |
| 15 | Bank Balances | ✅ Complete |
| 16 | Accounts Payable By Due Date | ✅ Complete |
| 17 | Gifts Pledges | ✅ Complete |

### Spec status key

| Badge | Meaning |
|-------|---------|
| ✅ Complete | The Step 5 deliverable for this widget is done — the spec is written and reviewed against the built Final and marked Complete at the project level (per direct instruction, 2026-08-04, for W01-W07). Note: these spec files still carry their internal `Status: DRAFT — not final` header and open sign-off items; that is an internal review/publish nuance kept deliberately, not a reason to treat the Step 5 stage as unfinished. |
| ✅ Approved | Sent out and done — the file's own `Status: DRAFT — not final` line has been removed per the process doc's own rule (mark draft while in progress, remove the line once approved — never version-numbered copies). No widget is at this state yet. |
| 🟡 Draft — needs review | File exists, still carries `Status: DRAFT — not final`, awaiting review. (Superseded for W01-W07 by ✅ Complete above.) |
| ⚪ Not started | Widget is eligible (Done in Step 4) but no `Step 5 - API documents/<Widget Name>/` folder exists yet. |

## Open gap

**Resolved 2026-07-27 — Pension Plans (W02) spec written.** This section previously flagged W02 as Done in Step 4 with no spec here; that spec now exists at [Pension Plans/Pension Plans - API Spec.md](Pension%20Plans/Pension%20Plans%20-%20API%20Spec.md) (🟡 Draft — needs review), written against `Widget_Comparison_Classic.html`'s `pp1` baseline (`PensionPlans` legacy class, `pension-plans` modern API) and the built Final. No other eligible widget is missing a spec: all five Step 4 ✅ Done widgets (W01, W02, W03, W04, W07) now have draft specs in this folder. Note: the tracker-status table above still shows W02 and W04 as ⚪ Not started from the last `Dashboard Tracker.xlsx` sync (2026-07-21) — both should read 🔵 In progress at the next tracker re-sync, matching W01/W07's draft-spec convention.

**Added 2026-07-28 — Remittance Pledges (W04) spec written.** W04's Final was built and its Step 4 doc tagged v2.0 (Step 4 Done) on 2026-07-28, making it eligible; its spec now exists at [Remittance Pledges/Remittance Pledges - API Spec.md](Remittance%20Pledges/Remittance%20Pledges%20-%20API%20Spec.md) (🟡 Draft — needs review). It captures the dev-confirmed calculation-only fix: pacing is per pledge term via `RM_Pledge.BeginDate`/`EndDate` (`Expected = TotalPledge × daysSinceBeginDate / totalTermDays`), replacing the two legacy calendar-year bugs (the hardcoded percent-of-year header and the Annual/12 × month-number YTD Expected), with `BeginDate`/`EndDate` already read by the query so no schema or query change is needed. The day-based status bands stay a frontend presentation concern (the API returns raw numbers). Real open question flagged: linear-by-days vs stepped-by-payment-schedule using `Frequency`/`Duration`. This corrects the earlier "W04 not eligible" note (W04 is Step 4 Done now per the build).

**Added — Receivable Invoices Outstanding (W05) spec written.** W05's Final was built (Step 4 Done as of the built Final), making it eligible; its spec now exists at [Receivable Invoices Outstanding/Receivable Invoices Outstanding - API Spec.md](Receivable%20Invoices%20Outstanding/Receivable%20Invoices%20Outstanding%20-%20API%20Spec.md) (🟡 Draft — needs review), the **6th widget** with a draft spec. It is structured as three APIs (owner-confirmed): API 1 widget aging summary with the one new field `invoiceCount` per bucket; API 2 pop-up bucket invoice list plus the project's first WRITE action (Confirm / move-to-unposted); API 3 per-invoice drill-in detail. The move-to-unposted transaction type is the central open SME/API item (Q1) and is deliberately not invented in the spec, per `Receivable Invoices Outstanding/Move to Unposted Transactions - Logic Notes.md`. This corrects the earlier "W05 not eligible" note.

**Added 2026-07-30 — Insurance Billing Plans (W06) spec written.** W06's Final was built (Step 4 Done as of the built Final, Jo design tagged v2.0), making it eligible; its spec now exists at [Insurance Billing Plans/Insurance Billing Plans - API Spec.md](Insurance%20Billing%20Plans/Insurance%20Billing%20Plans%20-%20API%20Spec.md) (🟡 Draft — needs review), the **7th widget** with a draft spec. It is a small read-only contract: two GET endpoints (plans, with an optional `typeId` filter, returning flat per-plan rows `{planId, planName, typeId, typeName, enrolled, cost}`; and the insurance-type filter list `{typeId, typeName}` from `IBType`, Active only). `enrolled` is the existing live count (`COUNT(IBEmployeePlan)` + `COUNT(IBEmployeeDependent)`); the ONE new backend field is `cost` per plan = `SUM` of the enrolment Rate (`IBEmployeePlan.Rate` when `UseNonStandardRate`, else standard `IBPlanRate` by plan + `TypeElectionID`), confirmed in code. The Type→Plan grouping, type subtotals, Share, and grand total are all client-side. Two open nuances flagged, not resolved: the "total cost" definition (total premium vs employer share `EmployerBilled`/`RateIndividual` vs employee share, `PreTax`), and the count-basis-vs-cost-basis difference (dependents are in `enrolled` but `Rate` sits on `IBEmployeePlan` only). No mutation, no status/COBRA/pending field (does not exist in the real module), no coverage-tier breakdown (future). W06 has a Confluence dossier but no reconciliation file yet. This corrects the earlier "W06 not eligible" note.

## Known open item — resolved by reopening the spec (2026-07-21)

The previous version of the Payroll Distributions spec never addressed the pay-type/earnings-code breakdown (Regular, Vacation, OverTime, Sick, Double Time, Personal, Holiday, Misc, Other) that the widget's build and final design doc both need — it wasn't in the Tables section, the old-vs-new table, or the sign-off list, despite the spec being marked done. Per direct instruction, the whole spec was rewritten in place (same file, no version-numbered copy) to name this gap directly: the only field either codebase actually returns (`PR_HistoryCompensation.Name`, via `CompensationDistributionID`) doesn't confirm whether it's the Department dimension, the pay-type Category dimension, or something else entirely — see the new spec's "Flag" under Tables and its "Still needs sign-off" list. The all-departments view is fully specified and buildable; the single-department Category view is explicitly flagged as not buildable until that's resolved. Status reverted to 🟡 Draft — needs review, since a spec that names a real unresolved architecture question can't honestly stay marked done. **Update 2026-07-27:** that unresolved question is now closed with evidence — the Pay Type Breakdown Analysis proof file in the widget's folder confirms the grouping is the org-defined distribution labels (`PR_CompensationDistribution.Name`) and that a full pay-type breakdown is supported by existing `PR_HistoryCompensation` columns (`SubType`/`Hours`/`Rate`), no schema changes; the spec was updated the same day.

**Reopened 2026-08-24 — Remittance Pledges (W04) spec back to 🟡 Draft.** The W04 widget was rebuilt the same day (v3.0 to v3.2) as the EXCEPTION side of the pledge pair, W17 owning goal progress, and that changed the backend contract, so the "Complete at Step 5 — awaiting management sign-off" status set on 2026-08-19 no longer described the document. What changed in the spec: (1) a new **"Why v3 changed this spec"** section giving each change its driver, including the Edward Eoff SME call of 2026-08-10 and his instruction never to label the date control "fiscal"; (2) `receiptsThrough` replaced by a **`rangeStart`/`rangeEnd` pair** (kept as a deprecated alias), where `rangeEnd` is the sole pacing anchor and `rangeStart` only lower-bounds receipts and decides pledge scope; (3) a new **pledge-in-scope overlap rule** which partly resolves the old ended-but-unpaid drop-off item; (4) two new response fields, **`windowPaid`** and **`hasPledgeInRange`**; (5) an explicit warning **not to window-bound `paid`**, recorded because the frontend build made exactly that error and it made every pledge read as catastrophically behind; (6) a rule that any server-side total shortfall must sum per-row positive shortfalls, never net them; (7) a new **API 3, Pledge Payment Schedule**, for the skipped-payment drill, with the oldest-first allocation algorithm; and (8) a note that the previously-academic "linear vs stepped expected" open item is **now load-bearing**, because a skipped payment only exists if there are discrete scheduled payments to skip. New blocking open item: receipts-only rows need a query path that does not exist today (Edward verified live that the widget shows nothing without a pledge), awaiting the owner's accept-as-risk-or-drop decision.
